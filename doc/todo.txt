Python:
	Use StrEnumsz
UniqueSerializable
	Just use UUID references with weakref fields and master field in project
	Do not share _instance among classes
	Update field when loading already existing item
self.get_datasource(index)
Search # TODO FIXME
Fix sort for fields without __gt__
Fix sort for children
Show thousands separator in status bar
Qt python properties

Use >= dependencies
Add arguments with units to docstrings, including properties
Refactor tests docstrings
Mock tests?
Fix __pycache__ location
UniqueSerializable:
	Use Pydantic
	_instance thread safety



Electrical:
	Accumulate errors in property
	BUG: Test when one wireByCriteria is empty
	Grouping wire layout
	Buried cables
	Bare wire
	Overload
	Short circuit
	Rename cable?
















def decode_data(bytearray2):

			data = []

			ds = QDataStream(bytearray2)
			while not ds.atEnd():

				row = ds.readInt32()
				column = ds.readInt32()

				item = {}
				map_items = ds.readInt32()
				for i in range(map_items):

					key = ds.readInt32()

					value = ds.readQVariant()
					item[Qt.ItemDataRole(key)] = value

				data.append(item)

			return data
		
		bytearray2 = data.data('application/x-qabstractitemmodeldatalist')
		data_items = decode_data(bytearray2)